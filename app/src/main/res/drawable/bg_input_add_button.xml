<?xml version="1.0" encoding="utf-8"?>
<!-- Outline-only round + button shown beneath the input pill. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1dp"
        android:color="@color/ios_section_label" />
</shape>
