<?xml version="1.0" encoding="utf-8"?>
<!-- Section 卡片头部行: 标题 + 计数 + 折叠箭头。
     背景在代码里按是否展开切换 bg_outline_section_top (展开) / bg_outline_section_solo (折叠)。 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:layout_marginHorizontal="16dp"
    android:layout_marginTop="14dp"
    android:paddingStart="18dp"
    android:paddingEnd="14dp"
    android:paddingTop="14dp"
    android:paddingBottom="14dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:background="@drawable/bg_outline_section_solo">

    <TextView
        android:id="@+id/textSectionTitle"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="?attr/colorOnSurface" />

    <TextView
        android:id="@+id/textSectionCount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:textSize="13sp"
        android:textColor="@color/ios_section_label" />

    <ImageView
        android:id="@+id/imageSectionChevron"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:src="@drawable/ic_chevron_right"
        android:rotation="-90"
        app:tint="@color/ios_section_label" />
</LinearLayout>
